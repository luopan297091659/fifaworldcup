# 服务端规划

## 结论

当前项目已切换为正式服务端模式，不再使用本地 mock 数据兜底。

正式上线建议接入微信云开发或自有 HTTPS 服务，用最短路径支持登录、多人小组、预测记录和赛后结算。

## 为什么正式版需要服务端

- 用户登录：用微信 openid 识别用户。
- 好友小组：小组、成员、邀请关系需要多人共享。
- 预测记录：用户提交后要落库，截止时间后不能修改。
- 自动结算：比赛结束后统一算分，避免客户端作弊。
- 排行榜：好友榜、小组榜、全球榜都需要服务端聚合。
- AI 参考：AI 看法要提前封存，用户提交后再展示。

## 推荐架构

小程序端：

- 展示比赛、小组、排行榜
- 提交预测
- 发起分享

微信云开发：

- 云数据库保存用户、比赛、预测、小组、排行榜
- 云函数处理提交预测、结算积分、生成榜单
- 定时触发器拉取赛程、赛果、首发阵容并结算

第三方接口：

- 赛程和赛果 API
- 实际首发阵容 API
- AI 参考服务

## 首发阵容同步策略

首发阵容必须由服务端同步和封存，客户端只读服务端结果。

推荐规则：

- 开赛前 7 天到 24 小时：每 6 小时同步一次候选阵容和伤停状态。
- 开赛前 24 小时到 2 小时：每 1 小时同步一次阵容。
- 开赛前 2 小时到开赛：每 10-15 分钟同步一次，捕捉官方首发和临场调整。
- 开赛后：停止改写首发阵容，保留最后确认版本。

接口返回时带上：

- `lineups.home`
- `lineups.away`
- `lineupUpdatedAt`
- `lineupSyncStatus`：`fresh`、`stale`、`unavailable`

如果第三方接口失败，服务端返回上一版阵容并标记 `stale`，客户端仍允许用户手动填写首个进球球员。

## 第一阶段数据表

- `users`：用户资料、积分、称号、AI 对照数据
- `matches`：比赛、开赛时间、状态、赛果、AI 参考
- `match_lineups`：每场比赛两队首发阵容、球员位置、阵容更新时间
- `predictions`：用户预测、信心值、提交时间、得分
- `rooms`：小组名、类型、热度
- `room_members`：小组成员关系
- `rankings`：预计算排行榜

## 上线顺序

1. 接微信登录和用户表
2. 接比赛表和预测提交
3. 接首发阵容同步，比赛详情接口返回最新 `lineups.home/away`
4. 接小组与成员表
5. 接结算云函数和排行榜

## 微信登录接口

客户端启动时会静默调用 `wx.login`，并向 `/worldcup/login` 提交：

- `code`：微信临时登录凭证，服务端用它换取 `openid`/`unionid`
- `userInfo`：用户点击“微信一键登录”后才会带上昵称、头像
- `silent`：是否为启动时静默登录

接口建议返回：

```json
{
  "code": 0,
  "data": {
    "token": "server-issued-token",
    "user": {
      "id": "openid-or-user-id",
      "name": "微信昵称",
      "avatarUrl": "https://..."
    }
  }
}
```

客户端会把 `token` 存到本地，后续请求通过 `Authorization: Bearer <token>` 传给服务端。
