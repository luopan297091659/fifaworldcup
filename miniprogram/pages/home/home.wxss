.home-hero {
  position: relative;
  min-height: 600rpx;
  overflow: hidden;
  padding: 0;
  border-radius: 28rpx;
  background: transparent;
}

.hero-bg,
.hero-shade {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

.hero-bg {
  top: 18rpx;
  height: calc(100% + 36rpx);
  object-position: center top;
  pointer-events: none;
}

.hero-shade {
  background: linear-gradient(180deg,
    rgba(7, 12, 18, 0.18) 0%,
    rgba(7, 12, 18, 0.42) 38%,
    rgba(5, 9, 14, 0.72) 100%);
  pointer-events: none;
}

.hero-content {
  position: relative;
  z-index: 1;
  min-height: 560rpx;
  padding: 68rpx 24rpx 28rpx;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

.hero-title,
.hero-subtitle,
.hero-meta {
  position: relative;
  z-index: 2;
  color: #ffffff;
  text-shadow: 0 2rpx 8rpx rgba(5, 9, 14, 0.55);
}

.hero-meta {
  display: block;
  margin-top: 10rpx;
  color: rgba(255, 255, 255, 0.78);
  font-size: 24rpx;
}

.countdown {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10rpx;
  margin-top: 28rpx;
}

.countdown-item {
  min-height: 96rpx;
  padding: 14rpx 8rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.18);
  border-radius: 18rpx;
  background: rgba(8, 12, 18, 0.42);
  text-align: center;
  box-shadow: 0 8rpx 18rpx rgba(0, 0, 0, 0.18);
}

.countdown-value,
.countdown-label,
.stat-value,
.stat-label {
  display: block;
}

.countdown-value {
  color: #ffffff;
  font-size: 38rpx;
  font-weight: 800;
  line-height: 1.1;
  text-shadow: 0 2rpx 8rpx rgba(5, 9, 14, 0.55);
}

.countdown-label {
  margin-top: 8rpx;
  color: rgba(255, 255, 255, 0.86);
  font-size: 22rpx;
}

.hero-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
  margin-top: 18rpx;
}

.hero-stats view {
  padding: 16rpx 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.18);
  border-radius: 18rpx;
  background: rgba(8, 12, 18, 0.42);
  box-shadow: 0 8rpx 18rpx rgba(0, 0, 0, 0.18);
}

.stat-value {
  color: #ffffff;
  font-size: 34rpx;
  font-weight: 800;
  text-shadow: 0 2rpx 8rpx rgba(5, 9, 14, 0.55);
}

.stat-label {
  margin-top: 8rpx;
  color: rgba(255, 255, 255, 0.86);
  font-size: 22rpx;
}

.match-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.match-card,
.empty-card {
  padding: 22rpx;
}

.group-card {
  border: 2rpx solid #e7edf3;
}

.group-hero {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 6rpx;
}

.group-action-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 12rpx;
}

.mini-action-btn {
  min-height: 58rpx;
  padding: 0 14rpx;
  border: 2rpx solid #d2dae3;
  border-radius: 999rpx;
  background: #ffffff;
  color: #17212b;
  font-size: 22rpx;
  font-weight: 800;
  line-height: 1.2;
}

.mini-action-btn.primary {
  border-color: #0b7a75;
  background: #0b7a75;
  color: #ffffff;
}

.group-feed {
  margin-top: 14rpx;
  padding: 12rpx;
  border-radius: 14rpx;
  background: #f4f7fb;
}

.feed-item {
  margin-top: 6rpx;
  color: #304253;
  font-size: 24rpx;
  line-height: 1.45;
}

.group-note {
  display: block;
  margin-top: 10rpx;
  color: #68717d;
  font-size: 22rpx;
  line-height: 1.4;
}

.match-head,
.match-foot,
.room-preview {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.teams {
  display: grid;
  grid-template-columns: 1fr 80rpx 1fr;
  align-items: center;
  margin: 18rpx 0 14rpx;
  font-size: 36rpx;
  font-weight: 800;
  text-align: center;
}

.versus {
  color: #8a949f;
  font-size: 22rpx;
  font-weight: 800;
}

.status {
  color: #0b7a75;
  font-size: 24rpx;
  font-weight: 800;
}

.status.closed {
  color: #b64128;
}

.room-preview {
  padding: 20rpx;
}

.room-name {
  display: block;
  margin-bottom: 8rpx;
  font-size: 30rpx;
  font-weight: 800;
}

.rank-badge {
  min-width: 110rpx;
  padding: 12rpx 16rpx;
  border-radius: 8rpx;
  background: #17212b;
  color: #ffffff;
  font-size: 24rpx;
  font-weight: 800;
  text-align: center;
}

.empty-room .rank-badge {
  background: #0b7a75;
}
